import React from 'react'
import { StyledCreate } from '../styles/Create.styled'

export default function CreatePage() {
  return (
    <StyledCreate>
        <p>Unfortunately, time was not enough with food etc. (:</p>
    </StyledCreate>
  )
}
